# GDTracker — feature map (frontend)

Top-level routing lives in [`src/App.tsx`](../src/App.tsx). Unauthenticated users are sent to `/login`. After sign-in, users with no games are redirected to `/games/new` (full-screen create); otherwise they land on `/games` to pick or create a game. The main dashboard lives under `/g/:gameId/...` with a shared full-width header, plus a **left sidebar** navigation (icon-only) for Dashboard / Heatmap / Tasks / Archive / Feedback / Integration / Configuration; hovering or focusing an icon shows a tooltip label without shifting layout. Inner dashboard pages use flat `.gamePageSection` panels (see [`App.css`](../src/App.css)) inside a vertical `.gamePageStack` where a page has multiple blocks. The Tasks page uses a **left** filter rail and a main column for the table (filters stack above the table on narrow viewports). The Heatmap page uses the same **left** filter rail pattern for event-code filtering and **linked** chart axis range controls (default `[-100, 100]`, step 25) beside the chart (stacked on narrow viewports).

| Route                      | Page module                                                                                                           | Primary API modules (`src/api/`)                                                                                                                                                                                                                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/login`                   | [`src/pages/LoginPage.tsx`](../src/pages/LoginPage.tsx)                                                               | [`auth.ts`](../src/api/auth.ts), [`games.ts`](../src/api/games.ts)                                                                                                                                                                                                       | Sign-in and register (testing); then routes by game count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `/games`                   | [`src/pages/GamesHubPage.tsx`](../src/pages/GamesHubPage.tsx)                                                         | [`games.ts`](../src/api/games.ts), [`auth.ts`](../src/api/auth.ts)                                                                                                                                                                                                       | Lists owned games; if empty, redirects to `/games/new`. Quick-create form.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `/games/new`               | [`src/pages/GameCreatePage.tsx`](../src/pages/GameCreatePage.tsx)                                                     | [`games.ts`](../src/api/games.ts)                                                                                                                                                                                                                                        | Full-screen create; primes CSRF via `listGames()`; top-right **×** signs out and returns to `/login`; navigates to `/g/:gameId/dashboard` after success.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `/g/:gameId/dashboard`     | [`src/pages/DashboardPage.tsx`](../src/pages/DashboardPage.tsx)                                                       | [`gameExceptions.ts`](../src/api/gameExceptions.ts), [`features.ts`](../src/api/features.ts) (`listFeatures`, `listFeatureTaskProgress`, `archiveFeature`), [`configuration.ts`](../src/api/configuration.ts), [`tasks.ts`](../src/api/tasks.ts)                         | Split layout: **Features** tree + exception chart, bucket list, and detail. The features column is [`DashboardFeatureTreePanel`](../src/components/DashboardFeatureTreePanel.tsx) (shared with Archive). Feature list and task-progress load **independently** (a failing progress call does not hide features); errors show as banners. Features list uses [`featureTree.ts`](../src/util/featureTree.ts) (`flattenFeaturesForList` for show/hide subfeatures and per-row expand/collapse; `orderedFeatureTree` still used elsewhere; stranded nodes appended when hierarchy is broken). **Show/Hide subfeatures** dropdown and chevron toggles (same UX pattern as Tasks). Row main area opens **`FeatureTasksModal`** (`taskListScope` **active**); per-row **Archive** archives the feature and all subfeatures (`archiveFeature`). Task row navigates to Tasks with `?feature=&task=`. Exception detail: game exceptions may include optional **`shortErrorMessage`** (ingest/session) for compact labels; **`POST .../game-exceptions/{id}/reserve-task-index`** allocates a monotonic per-exception **EXCEPTION_INDEX**; **Create Task for Exception** applies templates ([`exceptionTaskTemplate.ts`](../src/util/exceptionTaskTemplate.ts), including **&lt;SHORT_ERROR_MESSAGE&gt;**) and navigates to Tasks with router state. When an active linked task exists, shows **Go to Exception Task** (green `.btnSuccess`, `/tasks?task=` for the oldest link by `createdAt`) instead of create. Deep link **`?exception=<gameExceptionId>`** selects the bucket and exception (then clears the query). Legacy `/g/:gameId/exceptions` redirects here. |
| `/g/:gameId/archive`       | [`src/pages/ArchivePage.tsx`](../src/pages/ArchivePage.tsx)                                                           | [`features.ts`](../src/api/features.ts) (`listFeatures` with `{ archived: true }`, `listFeatureTaskProgress` with `{ archived: true }`), [`tasks.ts`](../src/api/tasks.ts) (`listTasks` with `archivedOnly`, `archiveTask`)                                              | Split layout like the dashboard: **Archived features** tree (same panel as dashboard; no row archive action) + **Archived tasks** via embedded [`TasksPageBody`](../src/pages/TasksPage.tsx) (`archivedOnly`, `layout="embedded"`). Sidebar icon [`archive_page.svg`](../src/assets/icons/archive_page.svg). **`FeatureTasksModal`** uses `taskListScope="archived"` and deep-links to `/g/:gameId/archive?...`. Archived tasks and features do not appear on the main Tasks page, dashboard feature list, or Configuration feature list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `/g/:gameId/heatmap`       | [`src/pages/HeatmapPage.tsx`](../src/pages/HeatmapPage.tsx)                                                           | [`trace.ts`](../src/api/trace.ts) — `GET /api/games/{gameId}/game-trace` returns `GameEventTraceEntry[]` (optional `gameEventId`, `renderedMessage`, `definitionCode`, `definitionColor`); [`gameEvents.ts`](../src/api/gameEvents.ts)                                   | Trace heatmap plus **game events** table. Chart **X** = world **Z**, chart **Y** = world **X** (Godot vector from `location`). Both axes share a fixed **view domain** (default `[-100, 100]`, adjustable by **25** under the event-code filter; reset restores defaults). Chart height **80vh**. Left rail filters points by event definition code (plus “No event code” for traces without a linked definition). Scatter dots use each trace row's `definitionColor` (default when missing). Binned cells use that color only when every point in the bin with a valid color shares the same hex; bins mixing distinct colors keep the default accent fill. Hovering a point shows `Location <raw>` and the rendered message in definition color; hovering a cell shows `Count: N`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `/g/:gameId/tasks`         | [`src/pages/TasksPage.tsx`](../src/pages/TasksPage.tsx) (`TasksPage` → [`TasksPageBody`](../src/pages/TasksPage.tsx)) | [`tasks.ts`](../src/api/tasks.ts) (`archiveTask`, …), [`tags.ts`](../src/api/tags.ts), [`features.ts`](../src/api/features.ts), [`categories.ts`](../src/api/categories.ts), [`configuration.ts`](../src/api/configuration.ts), [`taskTree.ts`](../src/util/taskTree.ts) | Tasks CRUD with optional **parent task** (subtasks); optional **`sourceGameExceptionId`** when created from the dashboard; filter rail by feature, **category**, **tags** (multi-select; **ANY** / **ALL** match when two or more tags selected), and status; deep link **`?feature=<featureId>&task=<taskId>`** or **`?task=<taskId>`** opens the task modal and clears the query. Router state **`createFromException`** from the dashboard opens **create** with prefilled title/description/category after the **initial tasks-page load** (so categories and config are available for the default exception category). **create / view / edit** include parent picker and **tag** chips (filled pills). View/edit footer: **Archive** (active tasks only) beside **View exception in dashboard** when linked. Table: **Progress** (subtasks DONE/total); tree indentation, **Show/Hide subtasks**; **Tag** column (~2× **Category** width, wrapping pills). Root rows show feature/category; **tags** show on every row (including subtasks). Clicking a tag opens **`TagTasksModal`**. [`chipTextColor`](../src/util/chipTextColor.ts) picks label contrast. Cross-links to Configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/g/:gameId/feedback`      | [`src/pages/FeedbackPage.tsx`](../src/pages/FeedbackPage.tsx)                                                         | [`gameFeedback.ts`](../src/api/gameFeedback.ts)                                                                                                                                                                                                                          | **Feedback template** (meter definitions: question, field key, sort order) and **inbox** (list title + player id). Row opens a **modal** with full detail and meter values. Styles: `.feedbackPageSplit`, `.feedbackInbox*` in [`App.css`](../src/App.css).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `/g/:gameId/integration`   | [`src/pages/IntegrationPage.tsx`](../src/pages/IntegrationPage.tsx)                                                   | [`gameEvents.ts`](../src/api/gameEvents.ts) (`getIngestTokenStatus`), [`integration.ts`](../src/api/integration.ts) (`getIntegrationStatus`)                                                                                                                             | **Godot / HTTP** token check: copy-ready **GDScript** and **C#** for `POST /api/games/{gameId}/integration` with body `{"validation":"ok"}` (Bearer only, no `X-Player-Id`); link to **Configuration** for the ingest token. **Start check** records baseline `lastValidatedAt` and polls `GET .../integration/status` until a newer ping is stored. Validation panel: **idle** / **waiting** (throbber) / **ok**. Sidebar: [`integration_page.svg`](../src/assets/icons/integration_page.svg).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/g/:gameId/configuration` | [`src/pages/ConfigurationPage.tsx`](../src/pages/ConfigurationPage.tsx)                                               | [`configuration.ts`](../src/api/configuration.ts), [`categories.ts`](../src/api/categories.ts), [`tags.ts`](../src/api/tags.ts), [`features.ts`](../src/api/features.ts), [`gameEvents.ts`](../src/api/gameEvents.ts), [`tasks.ts`](../src/api/tasks.ts) (`TaskStatus`)  | Game configuration (feature flags, **settings** key/value table including optional `THEME_COLOR` as CSS hex for per-game `--accent` via [`GameThemeContext`](../src/context/GameThemeContext.tsx)), **exception task templates** (title + description patterns with placeholders, default category for tasks created from exceptions), product **features** CRUD with optional **parent feature** (subfeatures), **game event definitions** (template, **color**, ≤16×16 icon), **ingest token** regenerate, **categories** CRUD, and **tags** CRUD (name, color, optional description; live `.tagChip` preview). Legacy `/g/:gameId/features` redirects here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Shared**

- [`client.ts`](../src/api/client.ts) — shared Axios instance: credentials and CSRF header for Spring Security. After login, call a GET (for example `listGames()`) before POST so the `XSRF-TOKEN` cookie is set; [`GameCreatePage`](../src/pages/GameCreatePage.tsx) primes on mount.
- [`AuthContext`](../src/context/AuthContext.tsx) — session user and `login` / `register` / `logout`.
- [`GameDashboardLayout`](../src/components/GameDashboardLayout.tsx) — dashboard shell, brand row + sidebar (`GameIdProvider`, **`GameThemeProvider`** loads [`getConfiguration`](../src/api/configuration.ts) and applies optional `settings.THEME_COLOR` as `--accent`; cleared when leaving game routes), `.appShell` / `.appMain*` in [`App.css`](../src/App.css)), log out. Main content fills remaining viewport height below the header. Sidebar **Archive** uses [`archive_page.svg`](../src/assets/icons/archive_page.svg). **Feedback** uses an inline chat-bubble SVG in the layout file. [`FeatureTasksModal`](../src/components/FeatureTasksModal.tsx) lists tasks for a feature (same subtask tree, progress, and show/hide controls as the Tasks page) and links to the Tasks or Archive page via `taskListScope`. [`TagTasksModal`](../src/components/TagTasksModal.tsx) lists tasks that share a given tag (same scope). [`chipTextColor`](../src/util/chipTextColor.ts) utilities tag pill label contrast.
