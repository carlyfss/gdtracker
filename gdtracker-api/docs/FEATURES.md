# GDTracker API — feature map

Each row ties the domain concept to entity, persistence, HTTP layer, and the Liquibase migration that introduced or materially changed the table (see `migrations/` for follow-up alters).

| Domain | Entity (`model/`) | Repository | Controller | Migration(s) |
|--------|-------------------|------------|------------|--------------|
| **Users (dev auth)** | `User.java` | `UserRepository.java` | `AuthController.java` (`/api/auth/*`) | `0006_users_games_multitenancy.sql` |
| **Games (tenant)** | `Game.java` | `GameRepository.java` | `GameController.java` (`GET/POST /api/games`); post-create bootstrap via `GameSetupService` | `0006_users_games_multitenancy.sql` |
| **Categories (tasks)** | `Category.java` | `CategoryRepository.java` | `CategoryController.java` (`/api/games/{gameId}/categories`) | `0007_categories_and_game_configuration.sql` |
| **Tags (tasks)** | `Tag.java` | `TagRepository.java` | `TagController.java` (`/api/games/{gameId}/tags`) | `0014_tags_and_task_tags.sql` |
| **Game configuration** | `GameConfiguration.java` (`exception_task_template` JSON: title/description templates + default category id for tasks created from exceptions) | `GameConfigurationRepository.java` | `GameConfigurationController.java` (`GET/PATCH /api/games/{gameId}/configuration`) | `0007_categories_and_game_configuration.sql`, `0015_exception_task_template_and_task_source_exception.sql` |
| **Product features** (optional `parent_feature_id` for subfeatures; `archived` hides from default `GET /features` and dashboard progress; `POST .../features/{id}/archive` archives the feature and all descendants) | `Feature.java` | `FeatureRepository.java` | `FeatureController.java` (`/api/games/{gameId}/features`, optional query `archived`; `GET .../features/task-progress` optional `archived`; `POST .../features/{id}/archive`) | `0001_create_features.sql`, `0005_alter_features_add_description_status_color.sql`, `0006_users_games_multitenancy.sql`, `0012_features_parent_subfeatures.sql`, `0016_feature_task_archived.sql` |
| **Tasks** (optional `parent_task_id` for subtasks; optional `source_game_exception_id` FK to `game_exceptions`; same feature as parent; delete parent cascades to children; many-to-many `tags` via `task_tags`; optional `tagIds` on create/update — omit `tagIds` on PUT to leave tags unchanged; omit `sourceGameExceptionId` on PUT to leave exception link unchanged, send `""` to clear; `archived` and list query `archivedOnly`; `POST .../tasks/{id}/archive`) | `Task.java`, `TaskStatus.java` | `TaskRepository.java` | `TaskController.java` (`/api/games/{gameId}/tasks`; list supports `featureId`, `status`, `categoryId`, `sourceGameExceptionId`, `archivedOnly`, repeated `tagIds`, `tagMode` ANY/ALL; create/update body `TaskUpsertRequest` includes optional `parentTaskId`, `tagIds`, `sourceGameExceptionId`) | `0002_create_tasks.sql`, `0006` (features scoped), `0008_tasks_category.sql`, `0013_tasks_parent_subtasks.sql`, `0014_tags_and_task_tags.sql`, `0015_exception_task_template_and_task_source_exception.sql`, `0016_feature_task_archived.sql` |
| **Game events & definitions** | `GameEvent.java`, `GameEventDefinition.java` | `GameEventRepository.java`, `GameEventDefinitionRepository.java` | `GameEventController` + `GameEventDefinitionController` (session); `GameEventIngestController` — `POST .../game-events/ingest` (Bearer + `X-Player-Id`); `GameEventIngestService.java`; optional `game_player_id` on `game_events` | `0009_game_events_and_ingest_token.sql`, `0010_game_event_definition_color.sql`, `0017_game_players_feedback_ingest.sql` |
| **Game event traces (heatmap)** | `GameEventTrace.java`, `Vector.java` | `GameEventTraceRepository.java` | `TraceController.java` — `GET /api/games/{gameId}/game-trace` (session); `TraceIngestController.java` + `TraceIngestService.java` — `POST /api/games/{gameId}/game-trace/ingest` (Bearer ingest + header `X-Player-Id`, CSRF-exempt); body uses `GameEventTraceCreateRequest` (optional `gameEventId`); responses use `GameEventTraceResponse` (includes `gameEventId`, `renderedMessage`, `definitionCode`, `definitionColor`); optional `game_player_id` FK | `0003_create_trace_locations.sql`, `0006`, `0011_rename_trace_locations.sql`, `0017_game_players_feedback_ingest.sql` |
| **Game players (ingest identity)** | `GamePlayer.java` | `GamePlayerRepository.java` | `GamePlayerRegisterController.java` — `POST /api/games/{gameId}/game-players` (Bearer ingest **only**, returns `GamePlayerRegisterResponse`); `GamePlayerService.java` validates `X-Player-Id` on other ingests | `0017_game_players_feedback_ingest.sql` |
| **Integration ping** | `Game.java` (`last_integration_validation_at`) | `GameRepository.java` | `IntegrationController.java` — `POST /api/games/{gameId}/integration` (Bearer ingest **only**; body `IntegrationPingRequest`); `GET .../integration/status` (session, owner) returns `IntegrationStatusResponse`; `IntegrationPingService.java` | `0018_game_last_integration_validation.sql` |
| **Game exceptions** | `GameException.java` (`short_error_message` optional) | `GameExceptionRepository.java` | `GameExceptionController.java` (`GET` list/detail/interval, session `POST` report, `POST .../game-exceptions/{id}/reserve-task-index`); `GameExceptionTaskSequenceService.java` (JDBC upsert for per-exception monotonic index); `GameExceptionIngestController.java` — `POST /api/games/{gameId}/game-exceptions/ingest` (Bearer + `X-Player-Id`, body includes optional `shortErrorMessage`); optional `game_player_id` FK | `0004_create_game_exceptions.sql`, `0006`, `0017_game_players_feedback_ingest.sql`, `0019_game_exception_task_sequences.sql`, `0020_game_exceptions_short_error_message.sql` |
| **Game feedback** | `GameFeedback.java`, `GameFeedbackMeterDefinition.java` | `GameFeedbackRepository.java`, `GameFeedbackMeterDefinitionRepository.java` | `GameFeedbackMeterDefinitionController.java` — CRUD `/api/games/{gameId}/game-feedback-meter-definitions`; `GameFeedbackController.java` — `GET /game-feedback`, `GET /game-feedback/{id}` (session); `GameFeedbackIngestController.java` — `POST /game-feedback/ingest` (Bearer + `X-Player-Id`); `GameFeedbackIngestService.java`, `GameFeedbackQueryService.java` | `0017_game_players_feedback_ingest.sql` |

**Game event trace auto-create:** When `GameEventIngestService.ingest` saves a `GameEvent`, it inspects the normalized parameters; if both `location` and `map` (lowercase keys in the request, normalized to `LOCATION` / `MAP`) are present and non-blank, it persists a `GameEventTrace` linked to the new event. Failures during the auto-create are logged and do not propagate to the ingest caller.

**Ingest player header:** Ingest routes other than `POST .../game-players` and `POST .../integration` require `X-Player-Id` matching a row in `game_players` for that `gameId`. `GameEventIngestService` sets `game_player_id` on new `GameEvent` rows and on auto-created traces.

**Cross-cutting:** Shared ingest helpers live in `util/IngestHttpSupport.java` (Bearer extraction, `X-Player-Id`). `FeatureController` uses `TaskRepository` to enforce rules when deleting features that still have tasks. `CategoryController` uses `TaskRepository.existsByCategoryId` before delete for a clear **category has tasks** conflict. `GameSetupService` creates the default **Game exceptions** category and a `GameConfiguration` row when a game is created or when configuration is first requested and missing (idempotent). `GameAccessService` ensures the authenticated user owns the `gameId` on scoped routes. Request bodies for feature and task upserts use **`dto/`** records with Jakarta Bean Validation; **`ValidationExceptionHandler`** maps validation failures to HTTP 400 with the same style as other `ResponseStatusException` errors. **`ApiLoggingExceptionHandler`** logs `ResponseStatusException` at WARN and other uncaught exceptions at ERROR. **`ApiSecurityLoggingHandlers`** logs 401 entry-point and 403 access-denied (including CSRF) events at WARN.

**Security:** Passwords are stored in **plaintext for local/testing only** (`User.password`, `PlainTextPasswordEncoder`); do not use this configuration in production.
